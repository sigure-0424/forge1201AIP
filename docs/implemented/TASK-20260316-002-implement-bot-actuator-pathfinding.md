# TASK-20260316-002-implement-bot-actuator-pathfinding

## Objective
Implement a functional `bot_actuator.js` that connects to a local Minecraft server and integrates `mineflayer-pathfinder` with the `CreateModContraptionHazard` middleware.

## Scope
- Integrate `mineflayer` and `mineflayer-pathfinder` in `src/bot_actuator.js`.
- Connect the `CreateContraptionHazard` middleware to the pathfinder instance.
- Add a basic "Move To" command listener for bot control.

## Rationale
Building upon the core framework, the actuator needs to be functional to test the interaction between pathfinding and modded hazard avoidance.

## Execution Mode
`local`
