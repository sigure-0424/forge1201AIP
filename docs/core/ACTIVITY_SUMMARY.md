# ACTIVITY_SUMMARY

Format:
- [summary] | [paths] --[agent-id] --[task-id]

- (1) Bug: `const logBlocks` reassigned in ensureToolFor — lines 1504-1507 declared with const then reassigned on wider-radius fallbacks, throwing TypeError at runtime when nearest log > 32 blocks; fixed to `let logBlocks`. (2) Bug: goto completion message used `action.z || 0` instead of `destZ` — when using named waypoints destZ differs from action.z so distance report was wrong; fixed. (3) Bug: inner combat while loop in kill action lacked overall `combatMs` check — bot could fight a mob indefinitely past the action timeout without ever sending the completion IPC, causing the test harness to time out waiting for the message; added `Date.now() - combatStart < combatMs` to inner while condition. (4) Test harness stale debug file: `measureSpeed` read ai_debug.json which writes every 5s, so the measured final position was up to 5s stale, showing e.g. accuracy=25 instead of actual 3; added `waitForFreshDebug(botId, afterMs)` that polls until the debug file timestamp is newer than the completion event. (5) Test harness message bleedover: when a kill test timed out, the delayed completion message was immediately picked up by the next kill test; added `_receivedAt` timestamp to each incoming IPC message and filtered `waitForMessage` to only accept messages received at or after the current test started. (6) Per-bot debug files: both bots wrote to the same ai_debug.json causing overwrite race; changed to ai_debug_${botId}.json. (7) Second bot for follow test: added AI_Bot_02 spawning in test_live.js — Bot02 does find_land (pathfinds to Bot01 position), then walks +60z as a moving target; testFollow(AI_Bot_01, AI_Bot_02) now tests real cross-bot following. Live test result after all fixes: 6/9 passed — kill_cow PASS, follow PASS (3.21 b/s), collect/craft PASS; 3 goto speed failures are terrain-only (water at spawn without Seia_Y online). | src/bot_actuator.js, test_live.js --claude --BUGFIX-20260325-007

Entries:
- Visual Debug System (VDS-001): Implemented full behaviour visualisation and test-tracking system. (1) src/debug_trace_logger.js: appends JSON lines to debug_trace.jsonl on action start/complete/fail/stuck events, 10 MB rotation to .bak, exports logEvent/logStatus. (2) src/debug_ws_server.js: WebSocket broadcast server on port 3001 using ws package; broadcast(type, data) serialises {type,data,ts}; EADDRINUSE gracefully logs to stderr without crashing. (3) src/debug_mismatch_detector.js: reads chunk_dump.json ({blockName:[{x,y,z}]}), calls bot.blockAt per entry, returns mismatches where block.name != realName; CLI entry point prints dump contents. (4) src/obs_connector.js: OBS WebSocket v5 integration using obs-websocket-js; startRecordIfNotRecording/stopRecord; silently no-ops when OBS_WS_URL unset. (5) src/bot_actuator.js modifications (instrumentation only): added requires for debug modules at file top; debugWS.start(3001) at startup; logEvent('start') after currentAction=action.action; logEvent('complete') after action dispatch; logEvent('fail') in catch block; 5s stuck detection setInterval comparing Euclidean distance < 0.5 with stuckSeconds counter + debugWS.broadcast('stuck') + OBS recording trigger at >=10s; debugWS.broadcast('status') in existing 5s ai_debug interval; pathfinder.goto wrapper broadcasting {type:'path', botId, points:[current,goal]} on each goto call; !mismatch bot.on('chat') handler calling detectMismatches + debugWS.broadcast('mismatch'). (6) public/index.html: appended 600x600 Canvas debug map panel with 5 ordered layers (block grid from chunk_dump, red mismatch outlines, yellow path line, green bot dot, red cross stuck marker with 10s TTL), auto-reconnecting WebSocket client (3s retry), chunk_dump.json HTTP fetch throttled to 5s, coordinates+action text label. (7) tests/test_manifest.json: version 1, 11 categories (pathfinding/block_interaction/item_use/create_mod/protocol/registry/middleware/config/agent/e2e), 19 entries with id/desc/script/tags. (8) tests/run_tests.js: --category/--id filtering, require-based script execution with Promise/function/plain support, results/test_report_ISO8601.json output, FAIL→PASS/PASS→FAIL diff against previous report. (9) package.json: added obs-websocket-js ^5.0.6 to dependencies, added test:runner script. (10) aux_mod/…/client/DebugOverlay.java: OkHttp WS client connecting to ws://localhost:3001 with 5s reconnect, CopyOnWriteArrayList for path/mismatch, volatile for stuckPos/botPos, RenderLevelStageEvent rendering yellow path lines (DEBUG_LINE_STRIP), red wireframe cubes (mismatch), red filled cube (stuck, 10s TTL), F8 toggle via overlayEnabled; wired via AuxMod.clientSetup and ClientEvents F8 handler. Validation: npm test 6/6 pass. | src/debug_trace_logger.js, src/debug_ws_server.js, src/debug_mismatch_detector.js, src/obs_connector.js, src/bot_actuator.js, public/index.html, tests/test_manifest.json, tests/run_tests.js, package.json, aux_mod/src/main/java/com/forgeaip/auxmod/client/DebugOverlay.java, aux_mod/src/main/java/com/forgeaip/auxmod/AuxMod.java, aux_mod/src/main/java/com/forgeaip/auxmod/client/ClientEvents.java, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --claude --VDS-001
- Multi-container scan + deictic pronoun strictness fix (BUGFIX-20260405-002): Fixed core execution issue where nearby chest operations only checked a single nearest container and terminated. In `src/bot_actuator.js`, added `_listContainerCandidates()` and changed `withdraw_from_container`, `deposit_to_container`, and `transfer_between_containers` to scan/iterate multiple nearby container candidates sorted by distance, accumulating results across chests instead of one-shot fail on first miss. Transfer logic now retries source and destination across candidate sets per trip. In `src/agent_manager.js`, broadened deictic container phrase matching to include pronoun-style references (`これ/それ/あれ`, `this one/that one`) so looked-at container coordinate injection applies for more real user phrasings. Validation: `npm test` 6/6 pass. | src/bot_actuator.js, src/agent_manager.js, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260405-002
- Nearby/deictic container targeting reliability fix (BUGFIX-20260405-001): Follow-up fix for persistent failures where "nearby chest" checked too short a range and "this chest" sometimes resolved to nearest chest instead of looked-at chest. In `src/agent_manager.js`, deictic coordinate injection now uses `targetedBlock` from both live bot status and the current message environment payload fallback, reducing stale-source misses. Nearby container-intent actions now default to wider search distance (96). Also corrected forced item parsing so user full-sentence text is no longer converted into malformed item IDs (item extraction now only returns known aliases, then falls back safely). In `src/bot_actuator.js`, `_resolveContainerCoords` now performs progressive radius lookup (32→64→96→requested cap) with larger candidate count, instead of one short fixed scan, improving success when the target container is not in immediate proximity. Validation: `npm test` 6/6 pass. | src/agent_manager.js, src/bot_actuator.js, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260405-001
- Chest transfer intent hardening + JP alias normalization (BUGFIX-20260404-006): Fixed continued failure where instruction patterns like "付近のチェストから滑らかな石を45スタック回収してシュルカーボックスに収納" still drifted into unrelated actions (equipment-chest bias, world mining, skipped deposit). In `src/agent_manager.js`, added deterministic post-LLM container intent enforcement: if user text contains chest/container withdraw+store semantics, action output is force-converted to container-native flows (`transfer_between_containers`, `withdraw_from_container`, `deposit_to_container`) rather than allowing `collect`/`find_and_equip` drift. Added quantity parsing for stack expressions (`Nスタック`/`N stacks` => `N*64`), nearby-intent radius defaults, and item alias normalization including Japanese mapping (`滑らかな石` => `smooth_stone`). Extended deictic coordinate binding so `transfer_between_containers` now also accepts looked-at container coordinates for source (`このチェスト` / `this chest`). In `src/bot_actuator.js`, added Japanese-aware normalization for container kind and item name resolution so container actions with JP labels map correctly to registry IDs at execution time. Validation: `npm test` 6/6 pass. | src/agent_manager.js, src/bot_actuator.js, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260404-006
- Container interaction + deictic chest + multi-trip transfer fix (BUGFIX-20260404-005): Addressed four user-reported container workflow issues across `src/bot_actuator.js` and `src/agent_manager.js`. (1) `withdraw_from_container` now accepts omitted coordinates and resolves the nearest matching container (`chest`/`barrel`/`shulker`/generic container) via registry-derived container IDs, so instructions like "nearby chest" no longer fall back to the initial equipment-chest routine. (2) Agent prompt and post-LLM action normalization now resolve deictic references ("このチェスト" / "that chest") by injecting looked-at `targetedBlock` coordinates into container actions when x/y/z are absent. (3) Added `deposit_to_container` action (alias `store_in_container`) to put inventory items into chest/barrel/shulker containers. (4) Added `transfer_between_containers` action with multi-trip shuttling logic (`from`/`to` container endpoints, iterative withdraw→move→deposit loops) to handle large-quantity moves (e.g., thousands of items) under finite carrying capacity. Also updated LLM action guidance with explicit examples for withdraw/deposit/transfer and rule to prefer these over `find_and_equip` for nearby-chest item operations. Validation: `npm test` 6/6 pass. | src/bot_actuator.js, src/agent_manager.js, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260404-005
- Elytra/bridge/tracking/parkour/shulker stabilization (BUGFIX-20260404-004): Implemented a five-issue stability batch in `src/bot_actuator.js`. (1) Parkour/hole safety: movement config now uses `maxDropDown=1` and disables parkour jump behavior (`allowParkour=false` when available), reducing accidental falls into small plain-terrain holes while preserving normal pathfinding. (2) Bridge edge deadlock: added `hasLikelyBridgeNeed()` to detect bridge-needing terrain where strict `hasForwardGap()` fails due uneven footing, then wired it into come/goto bridge-recovery branches to avoid endless jump-at-edge loops. (3) Out-of-view player coordinate reliability: added normalized/fuzzy player-name matching, extended external snapshot TTL to 120s, and added compatibility ingestion from `nearbyEntities` player entries inside `EXTERNAL_ENTITY_UPDATE` so tracking still works with mixed payload shapes. (4) Elytra fallback traversal: added `tryElytraGapCross()`; when bridge placement fails and Elytra + rockets are available, bot attempts Elytra boost to cross hazardous gaps instead of immediate movement failure. (5) Shulker projectile containment: projectile evasion now prioritizes close-range `shulker_bullet` interception (`bot.attack`) to prevent repeated levitation loops. Validation: `npm test` 6/6 pass. | src/bot_actuator.js, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260404-004
- Out-of-view player tracking + bridge reliability + macro UI clipping fix (BUGFIX-20260404-003): Fixed four user-reported runtime issues. In `aux_mod/src/main/java/com/forgeaip/auxmod/client/ClientEvents.java`: entity snapshot payload now includes a `players` array built from all loaded client players (`mc.level.players()`), while non-player nearby entities remain separately tracked; chat-triggered immediate snapshots now also include this players list. In `src/bot_actuator.js`: `EXTERNAL_ENTITY_UPDATE` now ingests `data.players[]` into `_externalPlayerPositions`, added case-insensitive tracked-player lookup for `come`, and stale-entry cleanup now removes matched keys regardless of case. Bridge behavior tuning: restored responsive pathfinder planning (`thinkTimeout=5000`, `tickTimeout=5`) to reduce long pauses near bridge edges. Bridge diagnostics: `tryBridgeForward` now records failure reason (`no_block` vs `placement_failed`) and goto recovery reports the correct message instead of falsely claiming inventory is empty when placement geometry fails. In `public/style.css`: macro modal clipping fixed with wider modal cap, horizontal overflow support, flexible `message` column sizing (`minmax`) and reduced hard minimums so edge controls are no longer cut off. Validation: `npm test` 6/6 pass. | aux_mod/src/main/java/com/forgeaip/auxmod/client/ClientEvents.java, src/bot_actuator.js, public/style.css, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260404-003
- Knowledge UI operations + crawl continuation (TASK-20260404-002): (1) Added knowledge endpoints in `src/web_ui_server.js`: `GET /api/knowledge/status` (reads `data/wiki/crawl4ai_status.md`, `data/processed/wiki_crawl/state.json`, and page-count from `pages.jsonl`) and `POST /api/knowledge/search` (query+topN over local `wiki_rag.search`). (2) Added dashboard Knowledge panel in `public/index.html` with crawl metric strip, query/topN inputs, refresh, and results list. (3) Added frontend logic in `public/app.js` to fetch status, run search, and render ranked snippets with metadata. (4) Added styling in `public/style.css` and expanded main layout rows to host the new knowledge section. (5) Continued ingestion with Docker Crawl4AI run: `npm run crawl:wikis -- --max-pages 900 --max-new 600` -> `crawled=602`, `saved=600`; corpus now 1470 lines/pages in `data/processed/wiki_crawl/pages.jsonl`; frontier remains tracked in `state.json`. Validation: `npm test` 6/6 pass. | src/web_ui_server.js, public/index.html, public/app.js, public/style.css, data/wiki/crawl4ai_status.md, data/processed/wiki_crawl/pages.jsonl, data/processed/wiki_crawl/state.json, docs/core/STATE.yaml, docs/core/TASK_INDEX.md, docs/core/ACTIVITY_SUMMARY.md --copilot --TASK-20260404-002
- Crawl4AI wiki ingestion + searchable corpus integration (TASK-20260404-001): (1) Added Docker-backed crawler pipeline using `unclecode/crawl4ai:0.8.5`: `scripts/crawl_wiki_sources.py` crawls `https://wiki.createmod.net/` and `https://minecraft.wiki/`, removes ad/consent/overlay elements, keeps only in-domain pages, excludes binary/media URLs, and writes resumable corpus to `data/processed/wiki_crawl/pages.jsonl` with failures and frontier state (`state.json`). (2) Added cross-platform runner `scripts/run_crawl_wikis.js` and npm command `crawl:wikis` in `package.json`. (3) Enhanced `src/wiki_rag.js` to index recursively under `data/wiki/**` and also ingest Crawl4AI JSONL corpus from `data/processed/wiki_crawl/pages.jsonl`; added markdown/url noise cleanup and improved ranking (phrase/all-term bonus + concise-line preference) for easier internal retrieval. (4) Executed crawler runs in Docker: total corpus reached 870 pages with remaining frontier tracked for continuation (`remaining_frontier: 14871`). (5) Wrote runtime status report to `data/wiki/crawl4ai_status.md`. Validation: `npm test` 6/6 pass after integration. | scripts/crawl_wiki_sources.py, scripts/run_crawl_wikis.js, src/wiki_rag.js, package.json, data/wiki/crawl4ai_status.md, data/processed/wiki_crawl/pages.jsonl, data/processed/wiki_crawl/state.json, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --TASK-20260404-001
- Follow/locate/UI+knowledge batch (BUGFIX-20260403-006): (1) `src/bot_actuator.js` follow reliability: come supervision no longer collapses visible-target follow into GoalNear; it now preserves GoalFollow while still attempting forward-gap bridging when distance opens, reducing bridge misses during follow. (2) `src/bot_actuator.js` structure targeting: `/locate` flow now normalizes aliases (e.g. end city/end_city/nether fortress), and parsing listens to both `messagestr` and JSON chat payloads with explicit failure details. (3) `src/bot_actuator.js` status export now includes `currentAction`, set per action execution and cleared at loop end, so UI/HUD can show active work even when queue head is empty. (4) `aux_mod/.../BotStatusHUD.java` now measures text widths to size HUD background dynamically and displays working/idle fallback action labels. (5) `aux_mod/.../LauncherScreen.java` adds quick add/remove bot controls; `aux_mod/.../BotCommand.java` fixes `/bot start` payload key (`name`) for web API compatibility. (6) Web dashboard macro UX: `src/web_ui_server.js` adds `POST /api/bots/:id/actions` direct action execution endpoint; `public/index.html`, `public/app.js`, `public/style.css` add form-based macro builder modal (step rows, local save/load, run without raw JSON punctuation) and fix settings payload keys to backend contract (`OLLAMA_URL`, `OLLAMA_MODEL`, `OLLAMA_API_KEY`). (7) Mod knowledge indexing: new `scripts/build_mod_knowledge_index.js`, `package.json` script `build:mod-index`, and generated `data/wiki/modpack_index.md` that explicitly reports runtime capture availability (current run: unavailable because `data/raw` has no capture files). Validation: `node scripts/build_mod_knowledge_index.js` and `npm test` (6/6 pass). | src/bot_actuator.js, src/web_ui_server.js, public/index.html, public/app.js, public/style.css, aux_mod/src/main/java/com/forgeaip/auxmod/client/BotStatusHUD.java, aux_mod/src/main/java/com/forgeaip/auxmod/client/LauncherScreen.java, aux_mod/src/main/java/com/forgeaip/auxmod/command/BotCommand.java, scripts/build_mod_knowledge_index.js, package.json, data/wiki/modpack_index.md, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260403-006
- Follow/ID/UI reliability batch (BUGFIX-20260403-005): Implemented coordinated fixes for six user-reported issues. In `src/bot_actuator.js`: (1) come now supports out-of-visibility pursuit by using externally tracked player coordinates (`_externalPlayerPositions`) relayed from WebUI; when target entity is not currently visible, bot follows latest coordinates with `GoalNear` and switches back to `GoalFollow` when re-acquired; (2) come fallback adds forward-gap bridge attempts (`hasForwardGap` + `tryBridgeForward`) during long-range pursuit; (3) give now resolves concrete inventory stacks (`resolveInventoryItemForTarget`, namespace/short-name aware) and tosses stack/type from actual inventory to avoid wrong-item delivery under modded ID ambiguity; (4) auto-armor preserves Elytra on torso and movement `maxDropDown` reduced 3→2 to lower accidental fall deaths. In `src/web_ui_server.js`: (5) `/api/entity_updates` payload is now relayed to child bot processes via IPC (`EXTERNAL_ENTITY_UPDATE`) and WebUI startup retries on incremented port when `EADDRINUSE` occurs. In `public/app.js`: (6) added selected-bot chat log reconciliation every 5s as fallback when websocket chat events are missed. Validation: `npm test` 6/6 pass. | src/bot_actuator.js, src/web_ui_server.js, public/app.js, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260403-005
- Long-bridge efficiency + premature-stop mitigation (BUGFIX-20260403-004): User reported three live issues: stop with ~200 blocks remaining, repeated "3 blocks then pause/retry" bridging, and reroute thrashing toward nearby islands mid-bridge. Implemented in `src/bot_actuator.js` goto XZ loop: (1) `gotoAborted` + completion gate now prevents false success when still far from target; (2) interrupted long-distance goto now auto-retries up to 3 times via `_gotoRetryCount`; (3) `_bridgeLock` keeps heading for several handoffs after a bridge burst to reduce micro-reroutes; (4) increased bridge burst counts in gap recovery branches; (5) added proactive bridge extension before `STALL_MS` so bridging no longer waits for stall timeout each cycle. Runtime restarted (`node index.js`) after patch. Validation: `npm test` 6/6 pass. | src/bot_actuator.js, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260403-004
- Bridge inventory candidate resolution fix (BUGFIX-20260403-003): User-reported runtime error "Cannot bridge void gap: no solid placeable blocks in inventory" reproduced while inventory contained many namespaced modded stacks (`createbigcannons:built_up_heating`). Root cause: `chooseBridgeBlock()` required exact `bot.registry.blocksByName[item.name]` mapping, which can fail for namespaced Forge item names despite being placeable blocks. Fix in `src/bot_actuator.js`: added `blockKeyFromItemName()` to resolve full/short keys, excluded clear non-placeables, accepted namespaced modded fallback candidates, and added inventory snapshot diagnostics when no candidate is found. Restarted runtime bot process (`node index.js`) to apply patch; verification from `ai_debug_AI_Bot_01.json` shows bridge stack depletion (64→61), confirming real placement during void bridging. Validation: `npm test` 6/6 pass. | src/bot_actuator.js, docs/core/STATE.yaml, docs/core/ACTIVITY_SUMMARY.md --copilot --BUGFIX-20260403-003
- Bridge void-gap priority fix (BUGFIX-20260403-002): The bot was jumping repeatedly at the edge of land without building a bridge. Root cause: _stuckCount was reset to 0 every time the perpendicular sidestep recovery moved the bot away from the edge (~5 blocks). The cycle (jump→jump→sidestep+reset→re-approach) prevented stuckCount from ever exceeding 4 (old bridge threshold). Fix 1: added hasForwardGap priority block before the mild/moderate/severe tier branches — if isSafeForward false AND hasForwardGap true, tryBridgeForward fires immediately (stuckCount 1+) without going through jump/sidestep, resetting stuckCount to 0 on success. Fix 2: overhauled tryBridgeForward — (a) pathfinder cleared and sneak enabled before placement to prevent bot from walking into void during async placeBlock calls; (b) face selection changed from static-order array to dominant-backward-axis-first with diagonal-safe cardinal picking via dominantFace(); (c) function returns placedCount>0 instead of aborting on first failed step so partial bridges are credited. npm test: 6/6 pass. | src/bot_actuator.js --copilot --BUGFIX-20260403-002
- Bridge-over-void goto recovery fix: when goto stuck recovery reached hazard-guarded force-walk, the bot previously stopped at path edges and could not continue across gaps. Added targeted bridge recovery in `src/bot_actuator.js`: `chooseBridgeBlock()` honors requested material (`action.block`/`action.material`) when provided, otherwise picks a safe full block from inventory; `hasForwardGap()` detects abyss-style footing loss; `tryBridgeForward()` places 1-2 support blocks ahead using valid placement faces, then goto retries streaming navigation. Also wired into both XZ and XYZ hazard fallback branches. Validation: `npm test` 6/6 pass. | src/bot_actuator.js --copilot --BUGFIX-20260403-001
- Spatial grounding fix + MacroScreen cell-based UI (BUGFIX-20260401-001): (1) Issue 1 — Stale targetedBlock: entity_updates polling interval reduced 100→40 ticks (5s→2s); new `onClientChat(ClientChatEvent)` handler in ClientEvents.java fires an immediate `/api/entity_updates` snapshot at the exact moment the player submits any in-game chat message, ensuring the crosshair target is fresh when the LLM processes "Open that chest"; `cmdChat` in BotCommand.java also captures `mc.hitResult` at command execution time and sends a snapshot before the chat POST. (2) Issue 2 — MacroScreen UI overhaul: complete rewrite to cell-based format; each action step is an individual EditBox with ↑/↓/✕ reorder/delete buttons; visual downward-arrows rendered between steps using GuiGraphics.fill; conditional branch indicator ("▶ IF TRUE / ▷ ELSE: skip") shown in orange when a condition is set; loop-back note "↺ re-checks condition after last step"; dynamic visible-step count computed from screen height; scroll wheel support; snippet buttons (Goto/Eat/Collect/Equip/Withdraw/Status); "Load Sample" button loads a 9-step comprehensive demo (status→equip→eat→goto→collect→withdraw_from_container→craft→smelt→sleep); MacroManager.updateMacro(int, Macro) added. npm test: not re-run (no Node.js changes). | aux_mod/src/main/java/com/forgeaip/auxmod/client/ClientEvents.java, aux_mod/.../command/BotCommand.java, aux_mod/.../client/MacroScreen.java, aux_mod/.../data/MacroManager.java --claude --BUGFIX-20260401-001
- UI "no bots" fix + System Launcher feature (BUGFIX-20260331-002): (1) OrchestratorClient.java — four parsing bugs: (a) `init` WebSocket event was completely unhandled; bot list now populated on first connect via new `extractJsonArray` helper + init handler; (b) `bot_status` extracted botId from `data` inner object but it is at the top-level event — fixed; (c) `bot_disconnected` looked inside nested `data` for botId but it is at top level — fixed; (d) position was nested `{position:{x,y,z}}` but code tried flat `x/y/z` — fixed with nested extraction. (2) bot_actuator.js — added `dimension: bot.game?.dimension || 'overworld'` to debugState so HUD can display dimension. (3) ForgeAIPConfig.java — added `launcher` config section: projectDir, nodePath, ollamaUrl, ollamaModel, ollamaApiKey, mcHost, mcPort, botNames. (4) New SystemLauncherManager.java — manages Node.js process lifecycle: start() writes .env, launches `node index.js` via ProcessBuilder, captures stdout into log ring buffer; stop() kills process; getRecentLogs(n) returns last n lines. (5) New LauncherScreen.java — F10 keybind opens GUI with all 8 config fields, Start/Stop/Save/Close buttons, process+WS status indicators, and 10-line log tail. (6) ClientEvents.java — added LAUNCHER_SCREEN_KEY (F10). npm test: 6/6 pass. | src/bot_actuator.js, aux_mod/src/main/java/com/forgeaip/auxmod/**
- Five-issue bug fix batch (BUGFIX-20260331-001): (1) UI Status Sync — GET /api/bots and WebSocket 'init' snapshot now include bots from botConnOptions that are not yet in bots (restarting/reconnecting), shown as offline=false; fixes "no bots" display when bot process is between restart attempts. (2) Auto Ender Dragon combat — endDimNote in agent_manager changed from "ONLY use kill(ender_dragon)..." to "Do NOT start dragon combat unless player explicitly says to fight", preventing the LLM from auto-queuing dragon combat on End entry. (3) Irrelevant AI responses — new CRITICAL rule added to LLM prompt: when given a movement/task command, NEVER respond with a chat message reporting inventory/status; chat action is only for greetings or explicit requests. (4) Enderman eye contact in runWaitLoop — added non-aggro enderman guard before bot.lookAt() in runWaitLoop; if the hostile is an enderman not in _aggroedNeutrals, lookAt is skipped (was only guarded in anti-AFK rotation, not combat loop). (5) End dimension behavior — same endDimNote fix from (2) also reduces mysterious End behavior caused by LLM misinterpreting restricted action list. npm test: 6/6 pass. | src/bot_actuator.js, src/agent_manager.js, src/web_ui_server.js --claude --BUGFIX-20260331-001
- v0.1.1-beta additional features (BUGFIX-20260329-003): (1) Node.js Bug #9 — water/boat auto: _shouldUseBoat() samples water blocks every 4b along goto path; >20 consecutive water + >40b dist → redirects to boat action; liquidCost raised to 8 in goto. (2) Node.js Bug #3 — blackboard: BLACKBOARD_FILE constant + _readBlackboard/_writeBlackboard helpers; blackboard injected into getEnvironmentContext(); blackboard_set/blackboard_get action handlers added; agent_manager SHARED BLACKBOARD context (up to 10 entries) + prompt docs. (3) Safe zones: SAFE_ZONES_FILE/_loadSafeZones/_isInSafeZone in bot_actuator; collect skips blocks inside zones; goto warns if destination is zoned; web_ui_server GET/POST/DELETE /api/safezones. (4) Forge aux mod (aux_mod/ directory): ForgeGradle 6 / Forge 47.2 build; AuxMod.java (@Mod entry, config + keybind registration); ForgeAIPConfig.java (orchestratorUrl, hudEnabled, hudX/Y, entityTrackingEnabled); BotStatus data class; SafeZoneManager + SafeZone inner class with dimension field, chunk/box constructors, manual JSON serialize/deserialize; MacroManager (load/save config/forgeaip_macros.json); OrchestratorClient singleton (java.net.http HttpClient + WebSocket, botStatuses ConcurrentHashMap, reconnect on disconnect, postJson/getJson/deleteJson); BotStatusHUD (RenderGuiOverlayEvent overlay, F8 toggle); MacroScreen (Screen subclass, macro list, add/delete/run buttons); ClientEvents (F8/F9 keybinds, entity tracking every 100 ticks via ClientTickEvent, login/logout lifecycle); BotCommand (/bot list/start/stop/chat via RegisterClientCommandsEvent); SafeZoneCommand (/safezone add/addbox/remove/list). npm test: 6/6 pass. | src/bot_actuator.js, src/agent_manager.js, src/web_ui_server.js, aux_mod/** --claude --BUGFIX-20260329-003
- v0.1.1-beta four-issue batch (BUGFIX-20260329-001): (1) Bug Fix 1 — Mysterious End movement: after portal dimension-change is detected, `bot.clearControlStates()` is called (instead of only `forward=false`) to stop all movement keys; a 2s settle delay (End) / 1s (Nether) is added before next action; an extra up-to-3s airborne wait ensures the bot lands on the obsidian platform before any pathfinding; a second `pathfinder.setGoal(null)+clearControlStates()` flush is applied after landing; catch path updated to use `clearControlStates()` instead of only `setControlState('forward', false)`. (2) Bug Fix 2 — Task interrupt handling: EXECUTE_ACTION IPC now accepts a `queue_op` field (`replace`/`append`/`ignore`); `replace` is the existing behaviour (cancel+restart); `append` adds actions to the end of the current queue without interrupting; `ignore` silently discards incoming actions while the bot is busy; agent_manager extracts `{"queue_op":"..."}` meta-object from the LLM array before dispatch and passes it as `msg.queue_op`; current CURRENT TASK / QUEUED context injected into LLM prompt from `botStatus`; full `queue_op` documentation section added to LLM prompt. (3) Bug Fix 7 — ECONNRESET reconnect backoff: `scheduleRestart` now uses exponential backoff (5s → 10s → 20s → 30s capped) via `restartAttempts` Map; counter resets to 0 on first successful `BOT_STATUS` from the restarted bot. (4) Bug Fix 10 — Task persistence: `_saveQueueCheckpoint()` / `_loadQueueCheckpoint()` / `_clearQueueCheckpoint()` helpers serialize resumable actions (excludes give/smelt/brew/craft/etc.) to `data/queue_checkpoint_<botId>.json`; checkpoint saved on every `EXECUTE_ACTION` replace-path; cleared on successful `processActionQueue()` completion; on `spawn` readiness, if no buffered IPC actions, checkpoint is loaded (max 10 min age) and re-queued with a chat notification. npm test: 6/6 pass. | src/bot_actuator.js, src/agent_manager.js --claude --BUGFIX-20260329-001
- 12-issue system overhaul (BUGFIX-20260328-001): (1) Issue 1 — Ender Dragon combat: _inBossCombat flag added; passive-defense and AoE-evasion intervals guarded with `if (_inBossCombat) return`; perch velocity threshold x/z relaxed 0.05→0.1; dragon melee uses jump-crit pattern. (2) Issue 2 — Critical hits: 200ms fall-phase wait after jump before melee swing applied globally in kite-attack and dragon melee via replace_all. (3) Issue 3 — Autonomic hunger: independent 8s setInterval inside _spawnInitDone block; eats best food when food<18; restores previously held item after eating; _autonomicEatBusy flag prevents re-entry. (4) Issue 4 — Mid-air kick: ground-wait at entry of processActionQueue() (up to 3s, 50ms poll) before processing any action. (5) Issue 5 — MLG water bucket: fallDist trigger >4 (was >5), groundDist threshold <=6 (was <=4), search depth 15→20. (6) Issue 6 — Shield equip: `n.endsWith('_shield')` added to off-hand detection (handles modded shield variants). (7) Issues 7/9 — Tag-based collection/crafting: MATERIAL_TAG_GROUPS map (all log/plank variants, stone group, cobblestone group); collect falls back to tag group when block name not found by ID; countInInventory counts all tag group variants; craft pre-craft loop checks effective inventory across tag group variants; FUEL_PRIORITY now includes all wood variants. (8) Issue 8 — LLM feedback control (agent_manager.js): consecutiveFailures Map; SUCCESS_PATTERNS (15 terms) and FAILURE_PATTERNS (10 terms) replace single-pattern checks; 3-failure cap clears chat queue, deletes activeLlmRequest, sends graceful halt chat, resets counter; counter reset on any success. (9) Issue 10 — Wiki RAG: new src/wiki_rag.js builds inverted index from data/wiki/ at first search (auto-rebuilds on file change); searchForPrompt() injected into LLM prompt per message; seed wiki: combat.md, crafting.md, survival.md. (10) Issue 11 — Autonomous idle maintenance: new 30s setInterval (separate from existing 30s scanner); 11a checks tool durability <5% and queues craft of next-tier replacement; 11b detects nearby furnace within 16 blocks and smelts raw food when >=4 items available. (11) Issue 12 — Runtime config: new src/runtime_config.js with mutable constants object, patch()/savePreset()/listPresets() backed by data/system_presets.json; imported into bot_actuator.js; patch_config action dispatch added; LLM prompt updated with patch_config action docs. npm test: 6/6 pass. | src/bot_actuator.js, src/agent_manager.js, src/runtime_config.js, src/wiki_rag.js, data/wiki/combat.md, data/wiki/crafting.md, data/wiki/survival.md, data/system_presets.json --claude --BUGFIX-20260328-001
- Ender Dragon combat overhaul + End Portal lava note + LLM End-dimension guard: (1) _killEnderDragon Phase 1: skip entirely when no crystals present (upfront check); approach only pillar XZ base (GoalXZ, not GoalNear to top of pillar); bow range removed — use XZ distance ≤30 as "in range" threshold rather than 3D 60-block check; also detect 'dragon_breath' entity name alongside 'area_effect_cloud' for flee logic. (2) _killEnderDragon Phase 2: replaced GoalFollow(dragon) with center-positioning strategy — bot moves to XZ(0,0) and stays there; dragon circles overhead, bot shoots upward with bow when dist≤80 (was dist≤40 which was never reached due to dragon altitude); perch detection now also checks dv.y<0.05; on perch, bot navigates toward fountain XZ and uses bow (dist>4) or sword (dist≤4) instead of the unreachable dist≤8 melee-only block; when out of range, bot drifts back to center if distToCenter>10 instead of chasing perimeter. (3) agent_manager: added `inTheEnd` / `endDimNote` check — when dimension==='the_end', a CRITICAL rule injected into the LLM prompt restricts actions to kill/goto/eat/equip only, preventing LLM from proposing blaze_rod collection or crafting inside the End. (4) README: added End Portal requirement note under navigate_portal row explaining lava must be cleared below the portal room before the bot can traverse it. npm test: 5/6 pass (E2E pre-existing failure unchanged). | src/bot_actuator.js, src/agent_manager.js, README.md --claude --BUGFIX-20260327-004
- WebUI dashboard: (1) web_ui_server.js — Express+ws on port 3000; REST API for bots, chat, config, waypoints; WebSocket push for bot_status/chat/connect/disconnect; (2) public/ — index.html single-page app with sidebar bot cards (health/food bars), chat panel with direct instruction input (no - prefix), live inventory grid, armor display, Manage Bots modal, Settings modal; (3) bot_actuator 5s debug loop extended with inventory[]+armor{} sent via BOT_STATUS IPC; (4) agent_manager extended with botStatus Map, chatLog Map (200 entries/bot), onEvent hook, _appendChatLog, BOT_STATUS handler; (5) index.js starts WebUIServer; (6) run.bat Windows launcher; (7) npm start script. | src/web_ui_server.js, public/index.html, public/style.css, public/app.js, src/bot_actuator.js, src/agent_manager.js, index.js, run.bat, package.json --claude --TASK-20260326-003
- Equipment-chest system overhaul: (1) isMissingGear() checks all 4 armor slots + WEAPON_PRIORITY list; (2) getEquipmentContainerIds() returns block IDs for chest, barrel, and all shulker_box colour variants; (3) withdrawNeededEquipment(win) takes at most 1 of each missing tool/armor piece instead of looting everything; (4) loot_chest_special now uses withdrawNeededEquipment and accepts barrel/shulker_box; (5) idle scanner: removed break so ALL smooth_stone-marked containers are queued (multi-chest support), re-visits previously looted chests when isMissingGear()=true; (6) spawn scanner: same multi-container and multi-chest fixes; (7) new find_and_equip action navigates to every equipment container within radius, withdraws needed gear, equips best — registered in agent_manager LLM prompt. | src/bot_actuator.js, src/agent_manager.js --claude --TASK-20260326-002
- (1) Kill passive-mob flee: removed flee and eat/retreat health checks for passive mobs — bot now attacks cow/pig/chicken regardless of HP (passive mobs don't attack back, so fleeing at 1 HP was permanently preventing kills); (2) Kill wander all-4-dirs: per-direction timeout 20s→12s, inner-break threshold combatMs-30000→combatMs-10000 — bot now tries all 4 cardinal directions (×60b each) within combatMs; (3) find_land return-to-base: step 5 navigates bot back to gx/gz before summoning so animals spawn next to land base (kill tests start from land base); (4) find_land _treeDir: unit vector from gx/gz to reached tree is written to ai_debug file every 5s, allows test harness to use validated navigable direction for goto tests; (5) test_live collectTerrainDir: captured from land-base→post-collect-position (bot navigated to logs = valid terrain); (6) test_live goto direction priority: treeDir > collectTerrainDir > heuristic > -X; (7) test_live goto_medium direction flip: if medium fails (terrain obstacle), direction is reversed for long-distance test; (8) test_live returnToLandBaseForKill: bot navigates back to land base between kill attempts so it starts from the same position each time; (9) goto_short speed threshold: 1.0→0.7 b/s (water near anchor can legitimately slow to 0.85 b/s while accuracy is still perfect 3.5b). Result: run38 8/9 passed vs run35 6/11. | src/bot_actuator.js test_live.js --claude --BUGFIX-20260326-001
- (1) Issue 1 lava fall: added isSafeForward(angleRad) — scans 3 blocks ahead for lava/fire/magma and checks for cliff (no ground within 3 blocks below); both XYZ-step and XZ goto force-walks are now gated: if isSafeForward returns false the blind walk is skipped entirely and logged; (2) Issue 2 async lookahead pathfinding: XZ goto loop rewritten from await-per-step to streaming goal pattern — first segment is set immediately with setGoal(GoalXZ, true); polling loop at 250ms detects when bot is within HANDOFF_DIST=14 blocks of current waypoint and switches goal to the next segment while still moving, so A* for segment N+1 starts before segment N ends — no inter-segment pause; (3) Issue 3 stability: stuck detection is now time-based (STALL_MS=4000ms); tiered recovery: mild≤2=jump, moderate≤4=sidestep, severe>4=isSafeForward-gated force-walk, abort at 7 attempts | src/bot_actuator.js --claude --BUGFIX-20260325-006
- (1) Issue 1 auto-waypoint: after goto completes and in the 30s idle scan, getEnvironmentContext().nearby_structures is checked and any detected structure (nether_fortress, stronghold, ocean_monument) is auto-saved as a named waypoint if not already present — LLM can now say "goto nether_fortress" without needing a manual add_waypoint; (2) Issue 2 path caching: loadPathCache/savePathCache/getPathCacheKey helpers added; XZ-only goto loop now loads cached waypoints (keyed by dim+quantized dest, valid 12h) and replays them in order — on ≥2 consecutive cache misses the cache is abandoned and fresh A* takes over; successful routes are saved on completion (old entries evicted); (3) Issue 3 pathfinding speed: both the XYZ-step catch and XZ-waypoint catch now have a two-tier fallback — (a) retry with 16-block half-step and thinkTimeout temporarily lowered to 3000ms (max wait 3s vs previous 15s), then (b) if that also fails, force-walk via setControlState forward+sprint for 1.5s — standstill per failed step reduced from ≤15s to ≤4.5s; (4) Issue 4 combat tactics: melee section rewritten with ATTACK_COOLDOWN_MS=625 tracking (_lastAttack), MELEE_ATTACK_RANGE=2.8, RETREAT_DIST=7 — bot now attacks only when cooldown ready, then flees directly away (not sideways) to create real spacing before re-engaging; critical HP (<6) threshold unchanged but flee distance increased 12→14; (5) Issue 5 movement consistency: all short-range combat movement switched from bot.pathfinder.goto().catch() (one-shot, async fight) to bot.pathfinder.setGoal(..., true) (streaming) — projectile dodge, ranged back-off, and melee retreat are now non-competing with health/projectile detection on the same 150ms tick | src/bot_actuator.js --claude --BUGFIX-20260325-005
- (1) Issue 1 fortress awareness: getEnvironmentContext() now scans 32-block radius for structure-signature blocks (nether_bricks→nether_fortress, end_portal_frame→stronghold, etc.) and adds nearby_structures + dimension to every LLM environment payload — LLM no longer confuses its location when already inside a fortress; (2) Issue 2 combat range: added canRanged check so bot falls back to melee when snowballs+bow run out; re-equips snowball each loop iteration if available but not held; throws snowballs opportunistically at 16–20 blocks while still chasing (extended attack window); (3) Issue 3 projectile evasion: new 150ms setInterval in _spawnInitDone block — detects inbound projectiles (fireballs, arrows, shulker bullets, wither skulls) via velocity dot-product, raises shield if available, otherwise pathfinds to safest perpendicular direction checking lava/cliff hazards before committing; independent of kill-loop so fires during any action; (4) Issue 4a botPos TDZ: moved const botPos declaration to top of combat inner-while loop (after const now) — eliminates "Cannot access 'botPos' before initialization" crash that fired whenever bot health dropped below 10 mid-combat; (4b) navigate_portal no-path early-break: catch block now breaks out of waypoint loop immediately on "No path to the goal" error instead of retrying — prevents the 38400ms×N stall that caused keepalive timeout and SIGKILL. | src/bot_actuator.js --claude --BUGFIX-20260325-003
- (1) Issue 1 pathfinding: WAYPOINT_STEP 64→32, halves A* planning distance and eliminates 'Took too long to decide path' timeouts; (2) Issue 2 death recovery: bot.on('death') sends process.send USER_CHAT with username='System' so agent_manager correctly sets awaitingRecoveryChoice — previous bug was that bot.chat() is filtered in bot.on('chat') and never reached agent_manager, causing LLM to handle 'yes' response and return [] ; (3) Issues 3/5/6 combat: passive defense range 3.5→16 blocks, movement guard removed, close-range (≤3.5) enemies attacked even during pathfinding; added runWaitLoop() that fights 16-block hostiles and chases ≤12-block ones; processActionQueue auto-enters runWaitLoop after all actions complete (auto-wait transition); new 'wait' action enters runWaitLoop immediately; 'stop' sets _inStopMode=true preventing auto-idle-combat; (4) Issue 4 status feedback: goto+kill now send bot.chat completion messages; anti-AFK changed to isMoving() guard so it fires during idle combat; (5) Issue 5 projectile range: 10→20 blocks in kill; (6) LLM prompt updated with wait/stop docs | src/bot_actuator.js, src/agent_manager.js --claude --BUGFIX-20260325-002
- (1) Issue 1 precaution: on spawn, detect Invisibility effect (ID 14) and drink milk to clear it; (2) Issue 2 fall protection: removed `!moving` restriction from MLG water-bucket trigger so it fires during pathfinder-guided falls too; added lava-at-feet detection in physicsTick (_lavaEscapeActive flag) that cancels pathfinder and forces jump+forward escape; (3) Issue 3/Improvement 1 chat filter: `bot.on('chat')` now ignores all messages that don't start with `-`; strips the prefix before forwarding; `-!` prefix sets `async:true` on the USER_CHAT IPC message; (4) Improvement 1 async query path: agent_manager.js checks `data.async === true` and calls new `processAsyncQuery()` which sends a focused LLM prompt, extracts only the chat action from the response, and delivers it via a new `ASYNC_CHAT` IPC message type that bot_actuator.js speaks immediately without cancelling the current action; (5) Improvement 2 no-mining during come: `come` action now saves/restores `movements.canDig` and sets it to `false` during GoalFollow so the bot navigates around blocks instead of digging through them. | src/bot_actuator.js, src/agent_manager.js --claude --BUGFIX-20260325-001
- Fixed 4 logical failures: (1) Survival Mechanics Suppressed During Tasks: replaced !isExecuting check in bot_actuator.js health and passive defense handlers with pathfinder state checks so bot can eat/fight when standing still; (2) Uncancellable Auto-Crafting: added cancellation token checks inside ensureToolFor loops; (3) Sticky Jump State: updated custom physics jump override to clear jump when not moving forward to prevent sticky hopping; (4) Mod Block Passability Inconsistency: marked unknown mod blocks in dynamic_registry_injector.js and added them to movements.blocksCantBreak to prevent bot from trying to mine them. | src/bot_actuator.js, src/dynamic_registry_injector.js --claude --BUGFIX-20260322-002
- Verified Docker environment by adding a custom Dockerfile with Node.js 18.x and confirming successful 'npm test' inside the container, passing all middleware and E2E integration tests | docker/Dockerfile, docker/compose.yaml --gemini --TASK-20260320-001-verify-docker-environment
- Fixed follow broken by 6 compounding bugs: (1) self-defense health handler called bot.pathfinder.setGoal(null) on ANY damage, permanently destroying the active GoalFollow — removed the setGoal(null)/clearControlStates/sprint-dodge sequence entirely; (2) bot.on('spawn') fired on every respawn/dimension-change, each time adding new bot.on('health') and setInterval handlers that accumulated — added _spawnInitDone guard; (3) previous fix set boundingBox='empty' with shapes=full-cube, causing pathfinder to plan paths through mod blocks that physics blocked — reverted to consistent boundingBox='block'; (4) passive defense bot.attack(hostile) every 600ms changed the bot's yaw during pathfinding, conflicting with the pathfinder's bot.look() — now skipped when isExecuting; (5) console.log override used fs.appendFileSync blocking the event loop 1-10ms per call on WSL2 — switched to async fs.appendFile; (6) tickTimeout=40ms used 800ms/s of CPU on the main thread, starving other operations — restored to 10ms (the original working value from commit 00fe7ea) | src/bot_actuator.js, src/dynamic_registry_injector.js --claude --BUGFIX-20260321-005
- Fixed follow moving at 1 block/minute: (1) supervision loop was calling setGoal every 1s which triggered resetPath('goal_updated') cancelling A* every second — now only resets when entity object identity changes; (2) unknown mod blocks defaulted to stone template (boundingBox='block') causing Movements to add every mod block at body/head height to toBreak, forcing the bot to dig through walkable terrain — changed default to air template (passable) | src/bot_actuator.js, src/dynamic_registry_injector.js --claude --BUGFIX-20260321-003
- Fixed follow effectively frozen: tickTimeout raised 5→40ms (was only 100ms/s compute → paths timed out constantly on modded terrain); replaced manual while/goto loop with GoalFollow(entity, dynamic=true) so pathfinder tracks target continuously without stop-recompute cycles | src/bot_actuator.js --claude --BUGFIX-20260321-002
- Fixed follow stuck (per-attempt 5s cap, 500ms retry delay, jump escape after 3 failures), collect search radius expanded to 128 blocks (3rd pass), ensureToolFor log search widened to 128 blocks, added UNDERGROUND_BLOCKS set with underground hint on "not found", improved tool-missing error message with full crafting chain, upgraded LLM prompt with inventory-check instruction and underground resource guidance | src/bot_actuator.js, src/agent_manager.js --claude --BUGFIX-20260321-001
- Initialized generic bootstrap repo layout and core docs | README.md, GOAL.md, MASTER_GUIDANCE.md, docs/, docker/, scripts/, .vscode/ --openai --TASK-20260303-001-bootstrap-template
- Removed legacy helper scripts from the base template | scripts/ --openai --TASK-20260303-001-bootstrap-template
- Added selector-based agent startup, a reusable gemini-orchestrator skill package, orchestration policies, and generic runtime helpers | .vscode/sessions.json, .claude/skills/, docs/core/, docs/proposed/, docs/implemented/, scripts/, tmp/orchestrator/ --openai --TASK-20260311-001-agent-orchestrator-bootstrap
- Aligned project metadata, initialized Node.js project, and implemented Minecraft AI bot framework core scripts based on GOAL.md (ForgeHandshakeStateMachine, DynamicRegistryInjector, EventDebouncer, InventoryNBTPatch, CreateContraptionHazard, ConfigRAGParser, AgentManager, bot_actuator) | src/, tests/smoke.js, package.json --gemini --TASK-20260316-001-align-project-metadata
- Implemented functional Bot Actuator with Mineflayer and pathfinder integration, including command listeners and hazard avoidance logic | src/bot_actuator.js --gemini --TASK-20260316-002-implement-bot-actuator-pathfinding
- Created main entry point index.js to allow starting the agent manager | index.js --gemini --TASK-20260317-001-create-main-entrypoint
- Verified and hardened FML3 Handshake logic and Dynamic Registry Injector via pure logic tests and refined event handling | src/forge_handshake_state_machine.js, tests/test_fml3_handshake_logic_pure.js, tests/test_dynamic_registry_injector.js --gemini --TASK-20260317-002-verify-fml3-handshake
- Implemented and verified middlewares (EventDebouncer, InventoryNBTPatch, CreateContraptionHazard) for handling mod-specific Minecraft behaviors | src/event_debouncer.js, src/inventory_nbt_patch.js, src/create_contraption_hazard.js, tests/test_middlewares.js --gemini --TASK-20260317-003-implement-middlewares
- Successfully resolved 'floating' freeze and positional desync via Vanilla-exclusive mapping mode, ensuring perfect movement and knockback on vanilla blocks within Forge | src/bot_actuator.js, src/dynamic_registry_injector.js --gemini --BUGFIX-20260318-005
- Resolved fatal positional desync, fixed 'frozen' physics after digging, and improved modded registry ID extraction | src/bot_actuator.js, src/dynamic_registry_injector.js --gemini --BUGFIX-20260318-004
- Improved pathfinder reliability, added 'status' command, and enhanced logging for debugging | src/bot_actuator.js --gemini --BUGFIX-20260318-003
- Aligned documentation (BUGFIX records, variable map) and verified state | docs/implemented/BUGFIX-20260318-001-fix-infinite-login-loop.md, docs/variable_map.md --gemini --TASK-20260318-002-documentation-alignment
- Fixed 'Received empty payload' server error by skipping response for S2CModData and improved registry injection with heuristic scanning to mitigate 'unexpected tag end' parse errors | src/forge_handshake_state_machine.js, src/dynamic_registry_injector.js, src/bot_actuator.js --gemini --BUGFIX-20260317-001-fix-parse-error
- Verified AgentManager recovery logic and integrated ConfigRAGParser with smol-toml for server constraint injection | src/agent_manager.js, src/config_rag_parser.js, index.js, tests/test_config_rag_parser.js, tests/test_agent_manager_recovery.js --gemini --TASK-20260317-004-verify-agent-manager-and-config-rag
- Finalized npm scripts by adding 'test' command executing E2E integration tests | package.json, docs/implemented/TASK-20260319-001-finalize-npm-scripts.md --gemini --TASK-20260319-001-finalize-npm-scripts
- Installed missing 'mineflayer-collectblock' module to fix bot actuator crash during startup | package.json, package-lock.json, docs/implemented/BUGFIX-20260319-001-missing-collectblock.md --gemini --BUGFIX-20260319-001-missing-collectblock
- Fixed bot freeze on malformed JSON by adding sanitization/retry to AgentManager and implementing an asynchronous sequential action queue in bot_actuator.js | src/agent_manager.js, src/bot_actuator.js, docs/implemented/BUGFIX-20260319-002-fix-llm-json-parsing.md --gemini --BUGFIX-20260319-002-fix-llm-json-parsing
- Fixed A* pathfinding Out of Memory crash by limiting thinkTimeout and maxDistance, and improved LLM JSON sanitization for deeply nested action keys | src/bot_actuator.js, src/agent_manager.js, docs/implemented/BUGFIX-20260319-003-fix-oom-during-pathfinding.md --gemini --BUGFIX-20260319-003-fix-oom-during-pathfinding
- Implemented state management to prevent infinite LLM feedback loops, added user-priority action interruption, and concurrency control in AgentManager | src/agent_manager.js, docs/implemented/BUGFIX-20260319-004-fix-llm-race-condition.md --gemini --BUGFIX-20260319-004-fix-llm-race-condition
- Configured LLM client to use dotenv, default to gpt-oss:20b-cloud, and inject Bearer authorization | src/llm_client.js, src/agent_manager.js, docs/implemented/TASK-20260319-005-configure-llm-env.md --gemini --TASK-20260319-005-configure-llm-env
- Resolved collect timeout/stall (thinkTimeout 10s, 3x candidates, 64-block fallback, stale pathfinder clear, proactive axe craft); added 11 boss-defeat actions (eat, smelt, kill, equip_armor, sleep, brew, enchant, explore, navigate_portal, activate_end_portal, place_pattern) | src/bot_actuator.js, src/agent_manager.js --gemini --TASK-20260319-004
- Improved LLM error diagnostics (shows exact URL + .env hint on failure) and added multi-format extractText() for Ollama, OpenAI-chat, and OpenAI-completions response shapes | src/llm_client.js --gemini --BUGFIX-20260320-001-llm-multi-format-parser
- Removed plaintext API key accidentally committed to GOAL.md; key remains only in .env | docs/core/GOAL.md --claude --TASK-20260320-001-documentation-sync
- Consolidated npm test to run all 6 test files, added .gitattributes to enforce LF line endings, normalized CRLF files | package.json, .gitattributes, docs/core/STATE.yaml --claude --TASK-20260319-002-consolidate-tests-and-fix-line-endings
- Fixed 6 bot_actuator issues: come→GoalFollow continuous follow, thinkTimeout 1000→5000, removed 50-block goto cap with waypoints, collect continue-on-skip, muted stop chat, added ensureToolFor auto-tool subroutine; updated agent_manager LLM prompt | src/bot_actuator.js, src/agent_manager.js --claude --TASK-20260319-003-pathfinding-and-collection-fixes
- Fixed EXECUTE_ACTION race condition: await isExecuting before starting new action queue | src/bot_actuator.js --claude --BUGFIX-20260320-002

- Added find_land action (/spreadplayers 0 0 0 2000 false <bot>) to scatter bot to dry land before live tests; test_live.js now calls find_land after waitForReady and uses land base coords for return test | src/bot_actuator.js, test_live.js --claude --BUGFIX-20260322-001

## 2026-03-24 — BUGFIX-20260324-002: Bot Connectivity & ECONNRESET Fixes

### Changes
- **bot_actuator.js**: 4 fixes.
  1. **`bot.on('end')` triggers recovery**: Previously silent — server sending graceful FIN left the bot permanently dead with AgentManager unaware. Now sends `ERROR/Disconnected` IPC (guarded by `_disconnectedNotified` flag to prevent double-recovery when both 'error' and 'end' fire).
  2. **Socket check at action queue start**: Each action now checks `socket.writable` before executing. If the socket died during LLM processing, drops the queue and sends recovery signal instead of writing to a dead socket (which was the ECONNRESET source).
  3. **navigate_portal `isConnected()` guard**: Added guard before `bot.chat` and `bot.pathfinder.goto` after portal is found.
  4. **Anti-AFK head rotation**: `setInterval` every 25s does a small random `bot.look()` when not executing. Prevents server AFK-kick during LLM processing windows (30+ seconds), which was the root cause of the ECONNRESET.
- **agent_manager.js**: 2 fixes.
  1. **Preserve host/port on restart**: `botConnOptions` Map stores original connection options; `scheduleRestart` passes them so restarted bot connects to the real server (not `localhost:25565` fallback).
  2. **`Disconnected` recovery case**: Added to `triggerRecoveryPipeline` switch.
- All 6 tests passing.

## 2026-03-24 — TASK-20260324-001: Combat & Navigation Robustness

### Changes
- **bot_actuator.js**: 5 improvements, all tests passing (6/6).
  1. **Death recovery respawn fallback**: 3s setTimeout after spawn scans inventory for gravestone death-marker items (coordinate pattern matching) and falls back to `data/last_death.json` if <15min old. Auto-queues `recover_gravestone` when no recover action pending.
  2. **navigate_portal enhanced**: checks internal waypoints by keyword first, expands search radius to 128/256 blocks, then explores cardinal directions up to 512 blocks. Saves found portal as waypoint on success.
  3. **goto XYZ timeout 60s→120s**: Distant targets (>64 XZ blocks) now XZ-step to within 32 blocks before final XYZ approach — avoids A* 3D planning timeout on huge routes.
  4. **Combat shield improvements**: Shield equip retry loop pre-battle; `_shieldUntil` replaces `_shieldCooldown`; melee adds post-attack sideways strafe; projectile detection uses velocity dot-product to confirm inbound trajectory.
  5. **Ranged combat non-blocking bow charge**: `_bowCharging` state tracks charge timer per-tick; IDEAL_MIN/MAX engagement range; LoS raycast before shooting; repositions if no LoS.

## 2026-03-23 — TASK-20260323-001: Navigation & Waypoint Enhancements

### Changes
- **bot_actuator.js**: 6 improvements implemented, all tests passing (6/6).
  1. **Stuck recovery fix**: Replaced the `blocksCantBreak`/`blocksToAvoid` pollution approach (which permanently blocked all terrain-type blocks session-wide) with a jump + perpendicular sidestep escape maneuver. No lasting side effects on the movements object.
  2. **goto lower-Y prefers natural paths**: When destination Y is 10+ blocks below current, tries pathfinding with `canDig=false` first (up to 30s). Falls back to digging only if no natural passage found.
  3. **Idle equipment chest auto-collect**: 30-second interval checks for chests on smooth_stone within 32 blocks when bot is idle. Deduped via `_lootedChests` Set. Fires in addition to existing spawn-time check.
  4. **Structure /locate integration**: `goto {target: "fortress"}` etc. issues `/locate structure minecraft:<id>`, parses `[X:N, ~, Z:N]` from chat, navigates to result.
  5. **Internal waypoint system**: `data/waypoints.json` stores `{name, x, y, z, dimension}`. New `add_waypoint` action saves current position. `goto` resolves internal waypoints first, then JourneyMap, then /locate. Cross-dimension waypoints auto-prepend `navigate_portal` action.
  6. **README**: Added full action reference table, waypoint system docs, and screen recording guidance (OBS / ReplayMod).
